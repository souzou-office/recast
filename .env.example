# このファイルを .env.local にコピーして値を埋めてください:
#   cp .env.example .env.local
# .env.local は git 管理外（各自で用意）です。

# Anthropic API キー（必須）。https://console.anthropic.com で発行
ANTHROPIC_API_KEY=

# 任意: アプリの URL（既定 http://localhost:3000）。通常は設定不要。
# NEXT_PUBLIC_APP_URL=http://localhost:3000
